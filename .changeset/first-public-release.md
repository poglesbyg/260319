---
"decidex": patch
"@decidex/core": patch
"@decidex/mcp": patch
---

First public release: CLI (`init`, `generate`, `capture`, `stats`, `scan`), MCP server for Claude Code integration, and multi-tool injection (Cursor, Copilot, Windsurf).
